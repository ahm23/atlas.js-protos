syntax = "proto3";
package atlas.storage.v1;

option go_package = "nebulix/x/storage/types";

message MerkleProof {
  bytes chunk = 1; // The 1KB chunk data
  uint64 path = 2; // Path bits for unbalanced tree
  repeated bytes siblings = 3; // Sibling hashes for Merkle path
}

message Proof {
  string challenge_id = 1;
  bytes data = 2;
  uint64 index = 3;
  uint64 path = 4;
  repeated bytes hashes = 5;
}

message StorageChallenge {
  string challenge_id = 1;
  string file_id = 2;
  string file_merkle = 3;
  string provider = 4;
  uint64 chunk_index = 5;
  uint64 created_height = 6;
  uint64 deadline_height = 7;
}
